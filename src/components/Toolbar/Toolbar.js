import React from 'react';
import classes from './Toolbar.css';


const toolbar = ( props ) => (
    <header className={classes.Toolbar}>
      <h1 className={classes.Header}>Slot Machine Game</h1>
    </header>
);

export default toolbar;
